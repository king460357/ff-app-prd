# FlareFlow · Android FCM 高优先级透传唤醒与常驻通知 · PRD（2026-05-14）

> 本目录：**真理源 `.md`** + 本 `README.md`。若存在 `_PRD.html` / `.docx`，均为从 `.md` 派生；**改需求只改 `.md`**，再按需重导 Word / 更新 HTML（HTML 须按根目录 `CLAUDE.md` 经 PM 确认）。

---

## 本需求特点（一行）

仅 Android：**FCM 纯数据透传**唤醒 → 走**现网**常驻通知逻辑；无新 App 页面、不弹营销推送条。

---

## 怎么打开

- 真理源：`20260514_Android_FCM高优先级透传唤醒与常驻通知_PRD.md`（已精简结构）。
- Word：同目录 `*.docx`（Pandoc 导出，见初版导出命令或自行 `pandoc -f markdown -t docx …`）。

---

## 本目录文件清单

| 文件 | 说明 |
|------|------|
| `20260514_Android_FCM高优先级透传唤醒与常驻通知_PRD.md` | **真理源** |
| `20260514_Android_FCM高优先级透传唤醒与常驻通知_PRD.docx` | Word 派生（若有） |
| `20260514_Android_FCM高优先级透传唤醒与常驻通知_PRD.html` | HTML 派生（若有，须与 `.md` 同步） |

---

## 回链

← 批次入口：[`docs/20260514/README.md`](../../README.md)
