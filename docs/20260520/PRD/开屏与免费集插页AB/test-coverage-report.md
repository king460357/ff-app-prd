# test-coverage-report · 开屏与免费集插页 AB

> 生成日期：2026-05-20 · 真理源：`20260520_开屏与免费集插页AB_PRD.md`  
> 修订：验收场景无 AC 编号；Logo **2 秒**；无缓存最多 **2+5 秒**后进首页

## 摘要

| 项 | 数量 |
|----|------|
| PRD Gherkin Scenario | 11 |
| 已派生 Playwright test | 7（骨架；预加载/会话上限等场景待补） |
| 默认 CI 可跑（无额外 env） | 4 |
| 需 fixture / 真机 | 3+（含预加载/上限类） |

## 开屏实验（S-01）· PRD → Test

| PRD 场景名（5.1.9） | Test | 状态 |
|---------------------|------|------|
| 参数0不播开屏广告 | `参数0不播开屏广告` | 骨架 · 需 `AB_SPLASH_PARAM=0` |
| 参数1有缓存仅开屏广告且无运营开屏 | `参数1有缓存仅开屏广告且无运营开屏` | 骨架 · 需 `AB_SPLASH_PARAM=1` |
| 参数1无缓存直进首页且无运营开屏 | `参数1无缓存直进首页且无运营开屏` | 骨架 · 需 `AB_SPLASH_PARAM=1` + `E2E_NO_SPLASH_CACHE` |
| 参数1展示后预加载下一发 | — | ⚠️ 预加载需 mock / 网络断言 |
| 当天达到展示上限不再弹 | — | ⚠️ 需 `max_per_session` mock |

## 插页实验（I-01）· PRD → Test

| PRD 场景名（5.2.8） | Test | 状态 |
|---------------------|------|------|
| 参数0播放免费剧集时不插页 | `参数0播放免费剧集时不插页` | 骨架 · 需 `AB_INTERSTITIAL_PARAM=0` |
| 参数1在第4集有缓存时插页 | `参数1在第4集有缓存时插页` | 骨架 · 需 `AB_INTERSTITIAL_PARAM=1` |
| 参数1第3集不插页 | `参数1第3集不插页` | 骨架 · 需 `AB_INTERSTITIAL_PARAM=1` |
| 无缓存不挡播放 | `无缓存不挡播放` | 骨架 · 需 `AB_INTERSTITIAL_PARAM=1` + `E2E_NO_INTERSTITIAL_CACHE` |
| 进入播放页必须预加载插页 | — | ⚠️ 预加载需 SDK mock |
| 同一集不重复插页 | — | ⚠️ 同集二次进入 |

## ⚠️ PRD 缺漏 / 待对齐

| # | 类型 | 说明 | 建议 |
|---|------|------|------|
| 1 | 待确认 | `immersion_free_int` 场景号与现网一致 | PRD 8.2 与数据对齐后关闭 |
| 2 | 研发深化 | fail_reason 枚举、placement id | 补影响范围广告矩阵 |
| 3 | 测试 | `data-testid` 未在 PRD 锁定 | UI/前端实现时回写 5.1.7 / 5.2.6 |

## 结论

PRD 两个实验 **分章自包含**，主路径与 AB key **可进入评审**。Playwright 为**占位骨架**，已与 PRD 对齐「参数 1 = 仅开屏广告、无运营开屏」。上线前须绑定 App harness 与参数 mock。建议：**C 接受** 当前 PRD，测试随研发迭代补全 selector。
