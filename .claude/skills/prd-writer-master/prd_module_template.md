# {章节编号} {功能名称} · 标准模板

> 复制此模板，把 `{}` 中的占位内容替换为实际内容。
> 每个核心功能都用此模板独立写一节。

---

## {章节编号}.1 功能说明

> 一句话回答：这个功能解决什么问题、面向谁、用于什么场景。

{在此处填写功能说明}

---

## {章节编号}.2 页面位置

| 项 | 说明 |
|----|------|
| 入口位置 | {例：在主导航 → 个人中心 → 头像区域} |
| 页面路径 | {例：/profile/avatar} |
| 所属模块 | {例：用户中心} |

---

## {章节编号}.3 页面结构

```text
{页面名称}
├─ 区域一：{区域名}（功能：{用途}）
├─ 区域二：{区域名}（功能：{用途}）
└─ 区域三：{区域名}（功能：{用途}）
```

---

## {章节编号}.4 UI 示意（ASCII 线框图）

```text
┌──────────────────────────────────────────┐
│ 页面标题                                  │
├──────────────────────────────────────────┤
│ {区域一内容}                              │
│                                          │
│ {区域二内容}                              │
│                                          │
│ [按钮一]              [按钮二]            │
└──────────────────────────────────────────┘
```

---

## {章节编号}.5 交互说明

### 按钮一：{按钮名称}

| # | 项 | 说明 |
|---|----|------|
| 1 | 默认是否可点击 | {是 / 否；如否，说明置灰原因} |
| 2 | 点击后是否弹窗 | {是 / 否} |
| 3 | 点击后页面状态变化 | {例：进入加载中 / 跳转新页} |
| 4 | 是否可重复点击 | {是 / 否} |
| 5 | 成功后去哪里 | {例：留在原页 / 跳转列表页} |
| 6 | 失败后提示什么 | {例：弹轻提示"操作失败"} |

### 按钮二：{按钮名称}

（同上）

### 弹窗 A：{弹窗名称}（如有）

| # | 项 | 说明 |
|---|----|------|
| 1 | 标题 | {弹窗标题} |
| 2 | 正文 | {弹窗正文} |
| 3 | 按钮 | {取消 + 确认} |
| 4 | 取消的结果 | {关闭弹窗，回到原状态} |
| 5 | 确认的结果 | {执行操作 + 后续跳转} |

---

## {章节编号}.6 状态说明

| 状态 | 何时出现 | UI 表现 |
|------|---------|---------|
| 空状态 | {例：列表为空时} | {例：友好图示 + "暂无数据" + 引导按钮} |
| 加载中 | {例：等待请求时} | {例：loading 图标} |
| 成功 | {例：操作完成} | {例：结果展示 + 轻提示} |
| 失败 | {例：请求失败} | {例：失败图示 + 重试按钮} |
| 部分失败 | {例：批量操作部分成功} | {例：列表标识 + 汇总提示} |
| 置灰 | {例：必要条件不满足} | {例：按钮置灰 + 悬停提示原因} |

---

## {章节编号}.7 异常与置灰

| 边界场景 | 用户感知 | 系统响应 | 提示文案 |
|---------|---------|---------|---------|
| 必填为空 | 按钮置灰 | 不发请求 | "请先填写必填项" |
| 文本超长 | 输入框边框变红 | 不发请求 | "标题不能超过 100 字" |
| 网络异常 | 加载中超过 10s 后失败 | 自动重试 1 次 | "网络异常，请检查连接" |
| 无权限 | 入口隐藏或禁用 | 拦截请求 | "您没有此操作权限" |
| 并发冲突 | 提示数据已被修改 | 拦截提交 | "数据已被他人修改，请刷新后重试" |

---

## {章节编号}.8 联动规则

> 说明本功能与其他模块的同步关系。

| 项 | 说明 |
|----|------|
| 数据来源 | {本模块数据来自哪里} |
| 同步时机 | {保存时 / 实时 / 定时} |
| 同步方向 | {单向 / 双向} |
| 冲突处理 | {以哪边为准} |
| 失败回滚 | {同步失败时是否回滚} |

---

## {章节编号}.9 数据规范

| 字段名 | 中文说明 | 类型 | 长度/范围 | 必填 | 默认值 | 校验规则 |
|--------|---------|------|----------|------|--------|---------|
| {field_1} | {说明} | string | 1-100 | 是 | 无 | 不能纯空格 |
| {field_2} | {说明} | enum | - | 是 | {值} | 枚举：A/B/C |

---

## {章节编号}.10 验收标准

> 每条标准必须可观察、可判定。

1. {例：页面首屏加载时间 ≤ 2 秒}
2. {例：必填项未填时，提交按钮置灰且无法提交}
3. {例：成功保存后 1 秒内显示成功提示}
4. {例：失败时显示明确错误文案，且包含重试按钮}
5. {例：网络异常自动重试 1 次后仍失败时，给出明确提示}

---

## 自检清单（写完后核对）

- [ ] 1. 功能说明是否清晰回答了"解决什么问题、面向谁、用于什么场景"
- [ ] 2. 页面位置是否明确，研发能立刻定位
- [ ] 3. UI 示意是否画出了核心区域
- [ ] 4. 每个按钮是否说明了 6 项
- [ ] 5. 每个弹窗是否说明了 5 项
- [ ] 6. 6 类状态是否都有说明（空/加载/成功/失败/部分失败/置灰）
- [ ] 7. 异常边界是否覆盖（空内容/超长/网络/无权限/并发/格式不符）
- [ ] 8. 数据规范是否完整（字段名/类型/长度/必填/默认值/校验）
- [ ] 9. 验收标准是否可观察、可判定（无"流畅、美观"等模糊词）
- [ ] 10. 是否触发了唯一规则源冲突（与其他章节是否重复定义）
